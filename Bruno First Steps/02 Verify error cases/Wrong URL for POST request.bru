meta {
  name: Wrong URL for POST request
  type: http
  seq: 3
}

post {
  url: {{protocol}}://{{baseUrl}}/api/v1/greeting
  body: json
  auth: none
}

body:json {
  {
    "name": "{{name}}"
  }
}

assert {
  res.status: eq 404
  res.headers['x-request-id']: eq {{uuid}}
}
