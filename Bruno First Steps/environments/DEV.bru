vars {
  ENVIRONMENT_PREFIX: DEV_
  protocol: http
  baseUrl: localhost:3000
  name: Dev Alf
  vaultBaseUrl: http://localhost:8200
  vaultSecretEndpoint: /v1/secret/data/bruno
  vaultSecretName: vault_password
}
